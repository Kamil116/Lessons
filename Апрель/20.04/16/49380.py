# # Не работает
# def f(n):
#     if n == 1:
#         return 1
#     if n > 1:
#         return (n - 1) * f(n - 1)
#
#
# print((f(2024) + 2 * f(2023)) / f(2022))

# Динамика
f = [None, 1]  # f[1] = 1, к f[0] никогда не обратимся, он нужен для нумерации с 1
for n in range(2, 2025):
    f.append((n - 1) * f[n - 1])

print((f[2024] + 2 * f[2023]) / f[2022])
